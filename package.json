{
  "name": "collection-storage",
  "version": "4.0.0",
  "author": "David Evans",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "scripts/build.sh $npm_package_version",
    "format": "prettier --write .",
    "test": "lean-test --preprocess tsc --parallel-suites src && tsc && prettier --check . && scripts/build.sh $npm_package_version"
  },
  "workspaces": [
    "src/core",
    "src/dynamodb",
    "src/mongodb",
    "src/postgresql",
    "src/redis",
    "src/sqlite"
  ],
  "devDependencies": {
    "@rollup/plugin-terser": "0.4.x",
    "@rollup/plugin-typescript": "12.x",
    "lean-test": "2.x",
    "prettier": "3.7.4",
    "rollup": "4.x",
    "rollup-plugin-dts": "6.x",
    "typescript": "5.9.x"
  }
}
